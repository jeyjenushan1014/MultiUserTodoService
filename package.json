{
  "name": "multi-user-todo-service",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/gateway",
    "apps/account-service",
    "packages/common",
    "packages/contracts"
  ],
  "scripts": {
    "build:contracts": "npm run build -w @todo/contracts",
    "build:common": "npm run build -w @todo/common",
    "build:gateway": "npm run build -w @todo/gateway",
    "build:account": "npm run build -w @todo/account-service",
    "build:packages": "npm run build:contracts && npm run build:common",
    "build": "npm run build:packages && npm run build:account && npm run build:gateway",
    "lint": "npm run build:packages && eslint apps/*/src packages/*/src --max-warnings=0",
    "test": "npm run test -w @todo/common && npm run test -w @todo/account-service && npm run test -w @todo/gateway",
    "typecheck": "npm run build",
    "check": "npm run lint && npm run typecheck && npm test",
    "migrate:account": "npm run migrate -w @todo/account-service",
    "migrate:account:down": "npm run migrate:down -w @todo/account-service",
    "compose:up": "docker compose up --build",
    "compose:down": "docker compose down"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/node": "^24.3.1",
    "eslint": "^9.35.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.42.0",
    "vitest": "^3.2.4"
  }
}